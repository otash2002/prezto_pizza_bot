services:
  db:
    image: postgres:15-alpine
    container_name: fastfood-db-prod
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    ports:
      - "5432:5432" # Bazaga tashqaridan ulanish uchun (ixtiyoriy)

  bot:
    build: .
    container_name: fastfood-bot-prod
    restart: always
    depends_on:
      - db
    ports:
      - "3000:3000" # Backend porti
      - "5555:5555" # Prisma Studio porti (Sizga aynan shu yetishmayotgan edi)
    environment:
      DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public"
      NODE_ENV: production
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_ID: ${ADMIN_ID}

volumes:
  postgres_data_prod: